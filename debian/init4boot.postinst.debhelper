# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall init4boot
	if grep -qs '^init4boot$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^init4boot$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
