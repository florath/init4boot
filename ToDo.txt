
* Need to fsck before mount root?

* Add to doc: No program is executed - because it is possible that the
  system is different.

* Add a ChangeLog file.

* Rename cleanup stage or add an additional before cleanup.

* Using tftp is a SPOF.

* The -k parameter is mostly useless these times.
